default:
  dora_dir: /work/u2614323/code/audiocraft-origin/rakec/result/logs/coef1_lr1e-4_len10
  partitions:
    global: debug
    team: debug
  reference_dir: /work/u2614323/code/audiocraft-origin/rakec/result/logs/coef1_lr1e-4_len10
darwin: # if we detect we are on a Mac, then most likely we are doing unit testing etc.
  dora_dir: /tmp/audiocraft_${oc.env:USER}
  partitions:
    global: debug
    team: debug
  reference_dir: /tmp
